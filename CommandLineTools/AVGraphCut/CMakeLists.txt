CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT( AVGraphCut )

SET ( MODULE_NAME AVGraphCut)
SET ( MODULE_SRCS AVGraphCut.cxx )

cipMacroBuildCLI(
    NAME ${MODULE_NAME}
    SRCS ${MODULE_SRCS}
    ADDITIONAL_TARGET_LIBRARIES ${MODULE_TARGET_LIBRARIES}
    ADDITIONAL_INCLUDE_DIRECTORIES ${MODULE_INCLUDE_DIRECTORIES}
    )