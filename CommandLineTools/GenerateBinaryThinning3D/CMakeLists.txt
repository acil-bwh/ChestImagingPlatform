CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT( GenerateBinaryThinnig3D )

SET ( MODULE_NAME GenerateBinaryThinning3D )

SET ( MODULE_SRCS GenerateBinaryThinning3D.cxx )

cipMacroBuildCLI(
    NAME ${MODULE_NAME}
    ADDITIONAL_TARGET_LIBRARIES ${MODULE_TARGET_LIBRARIES}
    ADDITIONAL_INCLUDE_DIRECTORIES ${MODULE_INCLUDE_DIRECTORIES}
    SRCS ${MODULE_SRCS}
    )
