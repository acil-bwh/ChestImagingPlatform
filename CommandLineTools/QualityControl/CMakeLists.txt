CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT( QualityControl )

SET ( MODULE_NAME QualityControl )

SET ( MODULE_SRCS QualityControl.cxx )

SET ( MODULE_TARGET_LIBRARIES
  ${ITK_LIBRARIES}
  ${CIP_LIBRARIES}
  )

cipMacroBuildCLI(
    NAME ${MODULE_NAME}
    TARGET_LIBRARIES ${MODULE_TARGET_LIBRARIES}
    SRCS ${MODULE_SRCS}
    )



