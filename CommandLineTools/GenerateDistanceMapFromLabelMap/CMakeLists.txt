PROJECT( GenerateDistanceMapFromLabelMap )

set(MODULE_NAME GenerateDistanceMapFromLabelMap)

set(MODULE_SRCS
)

cipMacroBuildCLI(
    NAME ${MODULE_NAME}
    ADDITIONAL_TARGET_LIBRARIES ${MODULE_TARGET_LIBRARIES}
    ADDITIONAL_INCLUDE_DIRECTORIES ${MODULE_INCLUDE_DIRECTORIES}
    SRCS ${MODULE_SRCS}
    )
